global:
  scrape_interval: 10s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - 127.0.0.1:9090

  - job_name: ai-gateway-mesh
    metrics_path: /metrics
    static_configs:
      - targets:
          - ai-gateway.ai-gateway-mesh.svc.cluster.local:8080
