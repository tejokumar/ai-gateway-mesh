apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: ai-gateway
  namespace: ai-gateway-mesh
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - ai-gateway.local
